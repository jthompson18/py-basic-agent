[pytest]
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests that hit external services/containers (pgvector, MCP)
    pg: tests that require PostgreSQL/pgvector
    mcp: tests that require MCP server
asyncio_mode = strict
